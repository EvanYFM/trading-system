# Codex Instructions

## 保证金金额口径

- 金额口径日报脚本为 `scripts/generate_margin_weighted_seat_report.py`，输出到 `output/margin_weighted_seat_report_YYYYMMDD/report.html`。
- 金额方向按“方向变化手数 × 当前一手交易保证金”计算；加多/减空为偏多金额，减多/加空为偏空金额。
- 保证金优先取东方财富期货保证金表，并在 `data/margin_reference.csv` 与 `data/margin_fetch_status.csv` 保留来源、更新时间、合约和覆盖状态；默认 7 天内复用缓存，只有设置 `FORCE_MARGIN_REFRESH=1` 时才强制刷新。
- 报告前台以净金额变动为主：机构合计排行只展示内资 + 外资后的净偏多/净偏空金额；边际矩阵、共振表和席位分拆均标明净金额变动，并用内资 + 外资 + 家人原始方向作为三方净变动。

本项目用于期货席位资金面跟踪。

## 工作原则

- 先验证网页是否能读取，再做分析或自动化。
- 席位名称以网页实际可返回持仓表的名称为准；用户简称只作别名。
- 只做与当前任务直接相关的最小改动。
- 修改或新增分析逻辑后，更新 `docs/codex-handoff.md`。
- Windows PowerShell 下不要使用 Bash 风格 heredoc；临时 Python 校验统一使用原生 `python -c "..."` 或项目已有脚本。
- 较大修改完成后先运行相关验证并检查 `git diff`；只暂存本次任务涉及的文件，提交到 `main` 并推送 `origin/main`，最后向用户报告 commit hash。不得顺带提交日报输出、抓取数据、缓存、账号状态或其他无关工作区改动。

## 当前分析口径

- 减空、加多视为偏多方向。
- 减多、加空视为偏空方向。
- 品种层面合并同一席位下所有合约。
- 内资、外资看作正向指标；家人席位看作反向指标。
- 日报重点观察反向共振品种，以及强共振品种最近 5 个可用披露日的持续/反转。
- 不再单独生成外资专题日报；两份正式日报统一使用内资样本：国泰君安、东证期货、永安期货、海通期货、浙商期货、中财期货、南华期货、申银万国、中信期货、光大期货、一德期货、瑞达期货、银河期货；外资：高盛期货、摩根大通、瑞银期货；家人：东方财富、徽商期货、方正中期、华安期货、中信建投、广发期货、民生期货、平安期货、中泰期货。机构合并专题在同一份报告中介绍内资、外资和家人共振情况。
- 不再自动生成旧全席位日报；每日正式输出只保留机构合并专题日报和保证金金额口径日报。旧 `scripts/generate_futures_report.py` 仅作为手动排查/兼容脚本保留。
- 本地研究工作站由 `scripts/build_research_dashboard.py` 构建，静态源文件位于 `web/research_dashboard/`，输出到 `output/research_dashboard/`。它只消费已有日报 CSV，不得在前端重新定义三方口径或绕过日报的数据质量状态。
- 研究工作站必须保留历史日期切换、商品/股指隔离和趋势来源日期；历史趋势不得显示成当日事实。用户确认 MVP 后，才把构建脚本接入每日自动化。
- 研究工作站长图总览统一承接机构专题与保证金日报的核心观察，并把每个交易日持久化为 `output/research_dashboard/data/snapshots/YYYYMMDD.json`。在用户明确确认网页版可替代日报前，两份日报继续作为数据生产和对账层，不得提前停用。
- `output/research_dashboard_v2_mockup/index.html` 只保留为设计参考；主站“品种详情”必须使用真实快照渲染行情、资金、席位、趋势、天气和历史路径。基本面、研报或其他尚未接入的数据只能显示“待接入/无数据”，不得复制概念稿示意数字。
- 不再输出单席位 40% 阈值观察；EC 集运欧线不纳入重点持仓观察。
- 看多/上涨/正向用红色；看空/下跌/负向用绿色。
- 2026 年中国法定节假日或调休放假日不生成日报；自动化在休市日应跳过抓取、报告生成和通知。
- 两份正式日报均不再输出共振地图或散点象限地图；机构合并专题和保证金金额口径日报承担主要可视化分析。
- 机构合并专题和保证金金额口径日报新增“期货资金潮汐”：当前底表没有行情涨跌字段时，先按资金净流入/流出 × 总持仓增/减判断增仓或减仓驱动；后续接入行情源后再升级为资金流入/流出 × 上涨/下跌四象限。
- 持仓数、保证金金额、内资/外资/家人变化应优先在同一行内用条形刻度、进度条或小型可视化比较相对强弱，避免只列纯数字。
- 机构专题的核心品种全景必须把当前净持仓与今日边际变化分列。参考外部报告时只吸收这一类清晰的存量/增量区分和紧凑视觉，不恢复用户已取消的共振地图。
- 机构专题农产品板块加入 AI 天眼当日和未来天气风险。天气预警是外部事实，席位三方净结果是交叉验证；两者必须分列，不得把天气预警直接写成涨跌结论。
- 保证金日报新增独立股指板块：`IH/IF/IC/IM` 展示三方净金额；趋势动物若能定位上证 50、沪深 300、中证 500、中证 1000、科创 50、创业板 50，则展示趋势温度与 `return1d` 原值。股指 CSV 与商品 CSV 分离，股指不得参与商品板块、共振统计或商品保证金覆盖率。
- 从 02 到后续所有涉及具体品种的分析板块，必须先按商品板块分组，再在板块下列出品种净偏多/净偏空；02 排行板块统一使用三方共振净结果，计算口径为“内资 + 外资 - 家人席位”，并对每个已出现的商品板块分别列出前三净多、前三净空，某侧为空时保留板块并写明“今天没有净多/净空品种”。02 卡片中只保留机构合计与家人反向拆解，不再单独列内资、外资排行。板块标题只列净多/净空品种，不再展示整体净多、净空金额或合计手数。板块口径：贵金属、有色金属、家人品种、黑色系、油化工、谷物饲料、油脂油料、农副软商、其他商品。黑色系合并原黑色矿钢和煤化工，包含焦煤、煤炭、铁矿石、螺纹钢、热卷、锰硅、硅铁、尿素、PVC；油化工包括原油、燃油、低硫油、沥青、LPG、甲醇、苯乙烯、纯苯、PX、PTA、天然橡胶、PP、合成橡胶、塑料、乙二醇、短纤、瓶片；谷物饲料为豆粕、菜粕、玉米、豆一、豆二；油脂油料为棕榈油、菜油、花生、豆油；农副软商为棉花、白糖、生猪、苹果、鸡蛋、红枣、20号胶；玻璃、纯碱、氧化铝、烧碱单独归入“家人品种”板块。淀粉 `CS` 因流动性偏低不参与日报及研究工作站统计。
- 趋势动物 API 快照脚本为 `scripts/fetch_trend_animal_snapshot.py`。只从 `TREND_ANIMAL_API_KEY` 环境变量读取密钥；每日调用一次，每次先读取官方接口、更新状态与字段计费，再抓取 10 个重点商品及所需股指的最小快照字段。费用不再作为调用前阻断条件；调用后读取当日账单，仅在当日实际消费超过 1 元时提醒。不得把密钥、含密钥 URL、账户余额、账单明细或实际消费金额写入代码、报告、日志或项目文档。
- A 股社区情绪脚本为 `scripts/fetch_zsxq_equity_sentiment.py`。每日生成保证金日报前，复用 `zsxq-cli` 安全登录态读取“趋势小程序”知识星球报告日内帖子、评论及 Nick 的当日发帖/回复；只保留脱敏摘要、作者标签、方向词命中和来源日期。股指板块必须把社区直接事实、Nick 观点和规则化风险偏好判断分列，并注明不等同于股指涨跌预测。不得落盘账号、Cookie、token、签名媒体 URL 或完整原帖；登录失败、数据缺失或无当日样本时明确降级，不沿用旧日情绪。
- 研究工作站的商品收盘价与当日涨跌幅统一来自东方财富期货主力页对应主力合约的报告日日线，由 `scripts/fetch_eastmoney_main_quotes.py` 写入 `data/eastmoney_main_quotes_YYYYMMDD.csv`；不得用趋势动物 `priceIndex` 或 `return1d` 替代收盘行情。趋势动物只承担趋势温度、强度等趋势事实。
- 研究工作站技术面试点脚本为 `scripts/fetch_eastmoney_technical_snapshot.py`，当前默认只抓取沪银 `AG`。脚本必须优先使用报告日行情文件中的主力合约，日线只展示 MA5/20/60；15 分钟与 60 分钟使用成交量、持仓量、包含处理、分型、简化笔和最近中枢，统一判断“偏多 / 中枢震荡 / 偏空”。增仓同向比减仓同向权重更高；支撑压力从 MA5/20/60 和有效中枢边界中选择最近位置。页面必须把行情事实、规则判断和数据限制分列；当前主力合约历史不得表述为复权连续合约，简化缠论不得表述为严格背驰或一、二、三类买卖点。技术判断只用于执行层交叉验证，不得覆盖基本面和资金面结论。
- `sites/research_dashboard/` 是研究工作站的私有 Sites 部署包装，仅同步 `output/research_dashboard/` 到 `public/dashboard/`；不得在部署层复制资金计算。默认保持 owner-only，任何公开或共享访问都需要用户明确批准。
- 保证金金额口径日报优先读取 API 生成的 `data/trend_temperature_YYYYMMDD.csv`，否则才读取 `data/trend_temperature_latest.csv`；必须新增“趋势温度与资金共振”模块。仅纳入 `温/热/沸/凉/寒/冻`，过滤 `平`。`温/凉`为左侧预警，`热/寒`为右侧确认，`沸/冻`为极端警戒；用三方净金额 `内资 + 外资 - 家人` 对照趋势方向，标注资金顺势、逆势或未验证，并后台保留 `trend_temperature_used.csv`。趋势动物 API 直接事实与资金解读必须分列；文档未定义单位的字段按原值展示。
- 每次更新主力趋势表格并输出结果后，同时汇总最近 3 个交易日的强共振品种：一类是内资、外资同向；另一类是内资、外资同向且家人反向后同向。若同一强共振品种连续 2 天及以上出现，必须单独提醒用户。

## 重点品种

当前固定重点品种：燃油、苯乙烯、碳酸锂、豆粕、鸡蛋、焦煤、沪金、沪银、欧线集运、生猪主力合约。

结构页口径：
- 只对上述重点品种做净多前 5、净空前 5 席位结构分析；欧线集运因奇货可查无可用持仓数据，不参与席位合计或结构页结论。
- 品种结构页交叉验证仅后台写入 CSV，不在 report 中单独展示。
- 结构页名称映射以奇货可查为准：沪金->沪金，沪银->沪银，欧线集运->集运欧线。

## 登录与安全

- 奇货可查结构页若未登录，优先使用 Chrome 插件/浏览器缓存中的登录态。
- Chrome 状态不可用时，才临时登录并通过环境变量传递会话 Cookie。
- 不写入或保存任何账号、密码、Cookie、令牌等敏感信息。
- 对空表优先检查是否是席位简称或品种结构页名称不匹配。
- 网页不同席位可能出现不同最新披露日期，结论必须标注日期。

## 交易系统提醒

- 当用户询问具体投资/交易决策时，先按 `docs/trading-decision-checklist.md` 检查是否完成交易系统分析。
- 先检查五问模块：基本面主要矛盾、流动性、仓位、叙事/共识、周期。
- 同时检查能力圈、安全边际、市场阶段、情绪周期和杠杆风险。
- 必须做共识检查、空仓测试、最强反方逻辑、24 小时反向指标和主观拥挤检查；主观拥挤至少看紧急感、趋势破圈、身边共识和持有人扩散，防止确认偏误、逆共识过早和情绪过热追单。
- 若缺少五问模块、能力圈/安全边际判断、反方论证、技术触发、资金面披露日期、逻辑证伪、止损止盈或匹配仓位，应明确提示“未完成系统分析”，并优先建议补数据或等待触发。
- 用户要求技术面分析时，使用 `docs/technical-analysis-framework.md`：先看大周期结构，再看中周期形态，最后看小周期触发；技术形态只负责执行，不单独作为第一性开仓理由。
- `docs/trading-philosophy.md` 是上层交易哲学；若用户出现追涨杀跌、亏损补仓、浮盈加仓、急于回本、连续亏损或单月大亏等描述，应优先提醒风险纪律和冷却复盘。

## 商品期货与期权过滤口径

- 机构专题只观察商品期货；保证金日报的商品主体同样排除股指、国债和外盘，但允许独立股指专栏读取 `IH/IF/IC/IM`，不得混入商品分析。
- 国债与外盘若后续需要，另做专题，不混入两份正式日报。
- 期权波动率 demo 脚本为 `scripts/generate_option_vol_report.py`，输出到 `output/option_vol_report_YYYYMMDD/report.html`。
- 期权 demo 可以尝试访问 OpenVLab market、行情 light 页面和 volatility analysis 页面；如果公开页面只返回动态前端壳或历史接口不可见，必须在报告中标注抓取状态，不得把截图样例或缺失数据伪装成实时确认数据。
